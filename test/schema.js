'use strict';

require('mocha');
var assert = require('assert');
var Schema = require('..');
var schema;

describe('schema', function() {
  beforeEach(function() {
    schema = new Schema();
  });
});

